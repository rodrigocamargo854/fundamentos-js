let valor
    // quando tento iniciar uma variavel que nao foi inicializada
    // undefined
console.log(valor)
    //Caso seja necessário zerar a variável colocar nullUndefined

valor = null
    // quando a variavel estiver null nao podemos acesar nenhum atributo