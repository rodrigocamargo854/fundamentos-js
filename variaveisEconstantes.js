//declarando variaveis e constantes
// identificadores a, e
//let permite que você declare variáveis 
//limitando seu escopo no bloco, instrução, ou em uma expressão na qual ela é usada. Isso é inverso da keyword var, que define uma variável globalmente
// ou no escopo inteiro de uma função, independentemente do escopo de bloco

var a = 3
let b = 4
console.log(a, b)

//declarando var, c pode redeclarar uma variavel mais que uma vez
//declarando let, vc nao pode declara-la mais de uma vez

// definindo constantes

const c = 100
console.log(c)